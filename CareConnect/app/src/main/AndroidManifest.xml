<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rfbonett.careconnect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LaunchActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EnterLocationActivity"
            android:label="@string/title_activity_volunteer_quick_start"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LogInActivity"
            android:label="@string/title_activity_log_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CharityActivity"
            android:label="@string/title_activity_charity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VolunteerTypeSelectionActivity" />
        <activity android:name=".CharityListActivity" />
        <activity android:name=".CharityContentDescriptionActivity"></activity>
    </application>

</manifest>
